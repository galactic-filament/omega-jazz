sudo: false
language: ruby
rvm:
  - 1
  - 2.0
  - 2.1
  - 2.2
  - 2.3
  - ruby-head
services:
  - postgresql

env:
  - ENV=travis
before_install:
  - psql -f ./db/container-files/docker-entrypoint-initdb.d/db.sql -U postgres
  - cd ./app
